[pytest]
asyncio_mode = auto
# This file must live in tests/, not the repo root: the integration's own __init__.py sits in
# the root (HACS content_in_root), so a rootdir of the repo makes pytest treat it as a package
# and import it without package context. Always invoke as `pytest tests`.
